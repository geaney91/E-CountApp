#ifndef WRITETOLOGFILE_H
#define WRITETOLOGFILE_H

#include <QString>
#include <QMainWindow>

class WriteToLogFile
{
public:
    WriteToLogFile();
    static void write_to_file(QString text);

signals:

public slots:
};

#endif // WRITETOLOGFILE_H
